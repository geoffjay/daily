<!DOCTYPE html>
<html data-controller="theme">
  <%= render 'partials/head' %>

  <body class="bg-cobalt-100 dark:bg-cobalt-800 text-slate-600 dark:text-slate-400 font-mono h-screen flex flex-col justify-between">
    <header>
      <%= render 'partials/flash' %>
      <%= render 'partials/nav' %>
    </header>

    <main>
      <%= yield %>
    </main>

    <footer class="relative bg-cobalt-100 dark:bg-cobalt-800 pt-8 pb-6">
      <%= render 'partials/footer' %>
    </footer>
  </body>
</html>
